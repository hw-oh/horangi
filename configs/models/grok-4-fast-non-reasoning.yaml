# =============================================================================
# Grok-4-Non-Reasoning (Fast) Model Configuration
# =============================================================================

# Model identifier - using LiteLLM native xAI support
model_id: litellm/xai/grok-4-fast-non-reasoning

# Model metadata
metadata:
  provider: xai
  name: grok-4-fast-non-reasoning
  release_date: "2025-07-09"
  size_category: None
  model_size: None
  context_window: 2000000
  max_output_tokens: 65536

# API settings
api_key_env: XAI_API_KEY

# Default parameters
defaults:
  temperature: 0.3
  max_tokens: 65536

# Benchmark-specific overrides
benchmarks:
  bfcl:
    use_native_tools: true
  swebench_verified_official_80:
    max_tokens: 65536

